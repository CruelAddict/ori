---
name: Github pipeline
on: [push]
runs-on: macos-latest
jobs:
  init:
    uses: ./.github/actions/init
  build:
    needs: init
    uses: ./.github/actions/make
    with:
      action: build
  test:
    needs: init
    uses: ./.github/actions/make
    with:
      action: test
