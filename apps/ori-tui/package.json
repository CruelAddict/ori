{
  "name": "ori-tui",
  "module": "src/index.tsx",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.tsx",
    "build": "./build.ts",
    "lint": "bunx biome check --reporter summary",
    "lint:fix": "bunx biome check --write",
    "postinstall": "patch -p1 -N -r- < patches/follow-redirects+1.15.11.patch || true",
    "test": "bun run vitest",
    "test:run": "bun run vitest run",
    "test:watch": "bun run vitest watch",
    "test:coverage": "bun run vitest run --coverage"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.5",
    "@types/bun": "latest",
    "@vitest/coverage-v8": "^4.0.16",
    "vite-tsconfig-paths": "^6.0.3",
    "vitest": "^4.0.16"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opentui/core": "0.1.62",
    "@opentui/solid": "0.1.62",
    "axios": "^1.7.7",
    "contract": "file:../../libs/contract/typescript",
    "fuzzysort": "^3.1.0",
    "pino": "^9.5.0",
    "solid-js": "^1.9.9"
  }
}
